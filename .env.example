VITE_API_URL=http://localhost:3001/api/v1

# URL publica del servicio Neon Auth de la rama correspondiente. La usan el
# frontend para el login y el backend para descargar el JWKS con el que verifica
# los tokens, asi que apunta siempre a la auth de la misma rama que DATABASE_URL.
VITE_NEON_AUTH_URL=
